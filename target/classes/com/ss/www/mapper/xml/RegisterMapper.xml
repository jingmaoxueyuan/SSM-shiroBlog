<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ss.www.mapper.RegisterMapper">
      <insert id="newly_IncreasedUser" parameterType="User" >
      	insert into user(username,password,sex,create_date) values(#{userName},#{passWord},#{sex},#{create_Date})
      </insert>
</mapper>